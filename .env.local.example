# For the map feature to work, you need a Google Maps API key.
# 1. Go to the Google Cloud Console (https://console.cloud.google.com/).
# 2. Create a new project or select an existing one.
# 3. Enable the "Maps JavaScript API", "Geocoding API", and "Places API".
# 4. Create an API key under "Credentials".
# 5. It's recommended to restrict the key to your domain and the enabled APIs.
# 6. Copy the key here.
# 7. Rename this file to .env.local

NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=""
